# Audioprism

## Start 

start the local environment replica

```bash
dfx start --background
```

install npm modules if not already installed

```bash
npm install
```

deploy the canister

```bash
dfx deploy
```